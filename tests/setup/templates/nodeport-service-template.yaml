apiVersion: v1
kind: Service
metadata:
  name: ${SERVICE_NAME}
  namespace: ${SERVICE_NAMESPACE}
spec:
  type: NodePort
  externalTrafficPolicy: ${EXTERNAL_TRAFFIC_POLICY}
  selector:
    app: ${APP_LABEL}
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
